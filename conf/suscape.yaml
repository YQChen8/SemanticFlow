
dataset_path: /dataset/public_dataset_nas2/av2/preprocess_v2/sensor
# checkpoint: logs/jobs/seflow-587/12-04-21-39/checkpoints/18_seflow.ckpt
checkpoint: /home3/hqlab/chenyq/project/SeFlow/logs/jobs/seflow-587/12-16-17-46/checkpoints/last.ckpt
av2_mode: test # [val, test]
save_res: False # [True, False]

leaderboard_version: 1 # [1, 2]
supervised_flag: True # [True, False], whether you use any label from the dataset

# no need to change
slurm_id: 00000
output: ${model.name}-${slurm_id}
gpus: 1
seed: 42069
eval_only: True
wandb_mode: offline # [offline, disabled, online]
defaults:
  - hydra: default
  - model: deflow